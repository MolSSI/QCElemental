name: test
channels:
  - defaults
  - conda-forge
dependencies:
    # Base depends
  - numpy
  - nomkl
  - python
  - pint=0.10.0
  - pydantic=1.0.0

    # Testing
  - pytest=4.0.0
  - pytest-cov
  - codecov
