name: test
channels:
  - defaults
  - conda-forge
dependencies:
    # Base depends
  - numpy
  - nomkl
  - python
  - pint

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip depends
  - pip:
    - pydantic # Conda-forge still in progress
    - deepdiff # Only used for testing some areas
