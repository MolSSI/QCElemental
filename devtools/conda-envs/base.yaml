name: test
channels:
  - defaults
  - conda-forge
dependencies:
    # Base depends
  - numpy
  - nomkl
  - python

    # Testing
  - pytest
  - pytest-cov

    # Pip depends
  - pip:
      # Base depends
    - pint

      # Testing
    - codecov
    - deepdiff
    - pydantic  # Will eventually be from conda-forge when we get that working
