name: test
channels:
  - defaults
  - conda-forge
dependencies:
    # Base depends
  - numpy
  - nomkl
  - python
  - pint
  - pydantic=1.0.0

    # Testing
  - pytest=4.0.0
  - pytest-cov
  - codecov
